node_modules/
dist/
out/
*.vsix
.vscode-test/
bundled/
.cursor/

# Legion's machine-local state. Wildcard so anything new (file-hashes,
# embeddings, snapshots, git-context-cache, etc.) is ignored by default.
.legion/